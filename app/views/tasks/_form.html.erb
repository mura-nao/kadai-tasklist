<%= form_with(model: task, local: true) do |f| %>
  <%= f.label :content, "タスク" %>
  <%= f.text_field :content %>
  
  <%= f.label :status, "進捗" %>
  <%= f.text_field :status %>
  
  <%= f.submit "入力" %>

<% end %>